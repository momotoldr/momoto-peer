{
  "name": "momoto-peer",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted PeerJS broker (WebRTC signaling) for Momoto.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "peer": "^1.0.2"
  }
}
